#pragma strict
var x : boolean;
function Start () {

}

function Update () {
x = renderer.isVisible;
 if (!(this.gameObject.renderer.isVisible)){
 Debug.Log(renderer.isVisible);
 	//Destroy(this.gameObject);
 	}

	
}